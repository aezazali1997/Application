import { makeStyles } from '@shared';

export const styles = makeStyles({
  btn: {
    backgroundColor: 'transparent',
    color: '#333',
    border: 'none',
    margin: '0 .5rem',
    '&:hover': {
      cursor: 'pointer',
    },
  },
});
