import { makeStyles } from '@shared';

export const styles = makeStyles({
  appBar: {
    position: 'relative',
  },
  list: {
    position: 'relative',
    top: 30,
  },
  item: {
    width: '100%',
    display: 'flex',
    justifyContent: 'space-between',
  },
});
